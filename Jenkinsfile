pipeline {
    agent any

    tools {
        sonarRunner 'SonarScanner'
    }

    stages {
        stage('Test') {
            steps {
                sh 'echo Scanner Installed'
            }
        }
    }
}
